services:
   fractal-indexer:
     build: .
     image: fractalbitcoin/fractal-indexer:v0.1
     entrypoint: ./fractal-indexer -api
     volumes:
       - ./conf:/data/conf:ro
       - ./data:/data/data
     labels:
       - "name=fractal-indexer-query"
     logging:
       driver: "json-file"
       options:
         labels: "name"
         max-size: "1g"
         max-file: "3"
